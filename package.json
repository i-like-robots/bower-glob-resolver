{
  "name": "bower-glob-resolver",
  "version": "1.0.0-beta.1",
  "description": "A resolver plugin for Bower which enables the use of multiple, external bower.json files in your project which can be found using a glob pattern.",
  "main": "index.js",
  "scripts": {
    "test:spec": "echo \"No test specified\" && exit 0",
    "test:prettier": "prettier --config .prettierrc.json --list-different **/*.js",
    "test": "npm run test:prettier && npm run test:spec"
  },
  "keywords": [
    "bower-resolver",
    "glob"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "glob": "^7.1.3",
    "semver": "^5.6.0",
    "tmp": "0.0.33"
  },
  "devDependencies": {
    "prettier": "^1.16.2"
  }
}
